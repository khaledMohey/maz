# انسخ هذا الملف إلى .env.production قبل npm run build (للرفع اليدوي فقط)
# أو ضع نفس القيمة في Netlify / Cloudflare Pages → Environment variables
VITE_API_BASE_URL=https://ضع-رابط-render-هنا.onrender.com

# Cloudflare Pages (Settings → Environment variables):
#   SKIP_DEPENDENCY_INSTALL = 1
#   NODE_VERSION = 20
# Build command: npm run build:pages
# Root directory: frontend
# Output directory: dist
